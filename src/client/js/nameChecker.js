export function checkForInput(inputText) {
    // console.log("::: Running checkForInput :::", inputText);
    if (inputText === "") {
        return false;
    } else
    {
        return true;
    }
}
